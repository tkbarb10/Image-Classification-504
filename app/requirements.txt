# App-specific dependencies for the Streamlit classifier
# Streamlit requires pandas<3, so this app uses its own environment
# separate from the main research project.
#
# Setup:
#   python -m venv app/.venv
#   app/.venv/Scripts/activate       (Windows)
#   pip install -r app/requirements.txt
#   streamlit run app/app.py          (from project root)

streamlit>=1.42.0
shap>=0.47.0

# Core data / ML
numpy>=1.24.0
pandas>=1.4.0,<3
scikit-learn>=1.3.0
scikit-image>=0.21.0
joblib>=1.3.0
scipy>=1.10.0

# Image handling
pillow>=10.0.0

# Plotting
matplotlib>=3.7.0
